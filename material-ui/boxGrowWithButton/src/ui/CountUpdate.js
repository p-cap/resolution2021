import { Typography } from '@material-ui/core'
import React from 'react'

export default function CountUpdate(props) {
    return (
        <Typography variant="h3">Box p=&#123;{props.count}&#125;</Typography>
    )
}